---
import { getLangFromUrl } from '../../i18n/utils';
import { SECTIONS_CONTENT } from '../../content/index.astro';

const lang = getLangFromUrl(Astro.url);

const { about: AboutContent } = SECTIONS_CONTENT[lang];
---

<section
  class='flex items-center justify-center px-5 pb-8 pt-14 md:pb-24 md:pt-40 bg-white dark:bg-dark relative z-10'
>
  <AboutContent />
</section>
