---
import { SectionHero, SectionExperience } from '@/components/sections/index.astro';
import { Header, Footer } from '@/components/shared/index.astro';
import { getLangFromUrl } from '@/i18n/utils';
import Layout from '@/layouts/Layout.astro';

const lang = getLangFromUrl(Astro.url);
---

<Layout title='Israel Larrondo | Portfolio'>
  <Header lang={lang} />
  <SectionHero lang={lang} />
  <SectionExperience lang={lang} />
  <Footer lang={lang} />
</Layout>
